/**
 * enum =>sama konsep nya dengan enum dart
 */

/**
 * numeric enum => berdasarkan index
 * string enum => berdasarkan string
 */

enum Month {
   JAN,
   FEB,
   MAR,
   APR,
}

enum Day {
   Sen = "Senin",
   Sel = "Selasa",
   Ra = "Rabu"
}
