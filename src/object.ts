/**
 * cara terbaik menggunakan 
 * object.
 * pertama tentukan dulu type object nya
 * kedua inject variabel dengan type yang
 * sudah di tentukan.
 */

/**
 * pertama
 */

type User = {name : string, age: number};

/**
 * kedua => buat object
 */

let user:User = {
   name : 'Lintang Js',
   age : 20
}